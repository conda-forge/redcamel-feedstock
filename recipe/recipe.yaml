schema_version: 1

context:
  name: redcamel
  version: "0.1.8"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/redcamel-${{ version }}.tar.gz
  sha256: 12f26ebaf20423c1840bca99681735e9fe4f68ad678bbd39eeef4785ba99aa20

build:
  number: 1
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  python:
    entry_points:
      - redcamel = redcamel.remi_gui:main

requirements:
  host:
    - python ${{ python_min }}.*
    - uv-build >=0.8.13,<0.9.0
    - pip
  run:
    - python >=${{ python_min }}
    - chemformula >=1.2
    - python-graphviz >=0.20
    - h5py >=3.11
    - ipywidgets >=8
    - ipympl >=0.9
    - matplotlib-base >=3.7
    - numba >=0.58
    - numpy >=1.2
    - plopp >=23
    - scipp >=23
    - scipy >=1.10

tests:
  - python:
      imports:
        - redcamel
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - "*"

about:
  homepage: https://codeberg.org/FlashREMI/RedCamel
  repository: https://codeberg.org/FlashREMI/RedCamel
  summary: GUI tool to simulate Reaction Microscope detector images.
  license: GPL-3.0-or-later
  license_file:
    - LICENSES/
    - "REUSE.toml"
    - "CITATION.cff"

extra:
  recipe-maintainers:
    - HLinde
